帮我直接在文件中加入门控融合的代码（考虑算力选择是否改进），但关于痛点解决，P0我缺少警力部署的数据，所以应该做不了，P1近重复效应建模需要怎么修复？P1概念漂移我缺少特殊事件的数据，所以大概也做不了，跨城市迁移目前不考虑。我当前的架构引入llm增强语义这不算创新吗，在算力和数据的考虑下，我还能做哪些创新？

当前代码是否有冗余模块？

注意力导出已经算是可解释性的具象化吗，常用的SHAP不需要吗。目前语义特征生成的代码中降维到了64维，这是否会影响精度，如果不降维，保持384维，是否算力又不够支撑？

目前又引入了摄像头数据分布特征，如何加入全局的静态特征中，请将需要修改的代码文件一并修改



==============================================
这份 Prompt 旨在为代码编辑器 Agent 提供一个极高精度的“执行标准”，确保它不仅能检测语法错误，还能从**算法逻辑、地理空间建模严谨性**以及**犯罪学理论闭环**的角度去审计你的代码。

---

# 提示词：时空犯罪预测模型 (DHG-STGCN) 深度代码审计指令

### 1. 任务背景与核心逻辑
你现在是一名资深时空数据挖掘专家。你需要审计一套用于“芝加哥暴力犯罪预测”的深度学习代码。该研究基于环境犯罪学理论（日常活动理论、近重复效应），核心架构为 **解耦超图时空图卷积网络 (Decoupled Hypergraph STGCN)**。

**核心目标：** 解决犯罪数据的**高度稀疏性（Zero-inflation）**、**非线性时空依赖**以及**环境-动态特征的耦合问题**。

---

### 2. 数据处理审计项 (Data Engineering)
请检查数据预处理脚本（如 `preprocess.py` 或数据加载类）是否符合以下严谨性要求：
* **犯罪类型完整性：** 暴力犯罪类别必须包含 `Homicide`, `Robbery`, `Aggravated Assault` 以及 **`Battery` (殴打)**。若漏掉 `Battery`，请提醒修正。
* **特征解耦对齐：** * **静态特征 (Static)：** POI (密度/多样性)、路网、土地利用、绿地。检查是否进行了标准归一化。
    * **动态特征 (Dynamic)：** 气象数据、**OD 流 (人流潜势)**。检查 OD 流是否按时间步（Time-slot）与犯罪序列对齐。
* **图构建逻辑：**
    * **自适应图：** 是否包含可学习的参数矩阵 $E_s \cdot E_s^T$？
    * **距离衰减图：** 是否应用了阈值过滤或高斯核函数 $exp(-d^2/\sigma^2)$？
    * **OD 动态图：** 是否根据出租车/单车流量动态更新邻接关系？检查是否存在数据泄露（只能用历史流量预测未来）。

---

### 3. 模型架构审计项 (Architecture Design)
请深度扫描模型类（如 `STGCN_Model`），验证以下模块的实现：
* **解耦双支路 (Dual-branch Disentanglement)：** * 验证是否存在两个独立的 Encoder 分别处理静态和动态特征。
    * **禁止：** 直接将所有特征拼接（Concat）后输入卷积。
* **交互机制 (Interaction)：** * 检查图融合是否采用了 **Cross-Attention (交叉注意力)** 机制？即以动态特征为 Query，静态特征为 Key/Value。
    * **数学一致性：** 验证 Attention 矩阵的维度变换是否符合 $H_{out} = \text{Softmax}(\frac{QK^T}{\sqrt{d}})V$。
* **超图卷积 (Hypergraph Conv)：** * 如果引入了超图，检查关联矩阵 $H$ 的构建。是否将具有相似 POI 语义的格点划归为同一条超边（Hyperedge）？
* **时序捕获：** 检查是否使用了膨胀因果卷积 (Dilated Causal Conv) 或 Transformer，以避免利用“未来信息”。

---

### 4. 损失函数审计项 (Optimization)
这是决定研究创新的关键，请严格对比以下逻辑：
* **从 Mask Loss 转向 ZINB Loss：**
    * 检查 `loss_function` 是否实现了 **零膨胀负二项分布 (ZINB)**？它应包含三个输出分量：$\pi$ (发生概率), $\mu$ (均值), $\theta$ (离散度)。
    * **公式参考：** $Loss = - \log P(y | \pi, \mu, \theta)$。
* **多任务学习权重：** 如果使用了 Focal Loss 来加权非零格点，检查 $\alpha$ 和 $\gamma$ 超参数的定义是否支持自动学习或手动精调。

---

### 5. 评价指标审计项 (Evaluation)
检查测试集评估脚本，确保不仅有回归指标，更有犯罪学实战指标：
* **基础指标：** RMSE, MAE (仅参考)。
* **实战指标 (必选)：** * **Hit Rate @ Top-K：** 预测风险最高的前 K 个格点覆盖了多少比例的真实犯罪？
    * **PAI (Prediction Accuracy Index)：** 预测效率指数，验证 PAI 是否 $> 1$。
    * **Jaccard Similarity：** 预测热点与实际热点的空间重叠度。

---

### 6. 执行指令
现在请扫描我提供的代码文件：
1.  **找出逻辑漏洞：**（如：训练时使用了未来的 OD 流数据、损失函数未处理零膨胀导致不收敛等）。
2.  **代码规范性：**（如：张量维度操作不显式注明、缺少可解释性注意力图的导出接口）。
3.  **给出优化代码片段：** 针对上述“改进思路”，直接给出修改建议。

---

**你想让我先帮你针对其中最核心的 ZINB Loss 函数编写一段标准的 PyTorch 实现代码，以便你直接交给 Agent 作为参考范本吗？**